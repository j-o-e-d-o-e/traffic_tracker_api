query departuresByRegion($req: PageRequest, $region: Region) {
  departures(req: $req, region: $region) {
    icao
    name
    flights {
      totalElements
      content {
        date
        airline {
          icao
          name
        }
      }
    }
  }
}
